#!/usr/bin/env bash
# Sync design-system Claude Code skills to ~/.claude/skills/
# Run after `git pull` to update skills on this machine: ./sync.sh
# Identical in effect to install.sh — named separately for clarity in the update workflow.

"$(dirname "$0")/install.sh"
